experiment_name: "v7_replication"
seed: 42
output_dir: "./results/v7"

data_collection:
  loader: "LegacyLoader"
  loader_params:
    version: "v7"

preprocessing:
  pipeline: []

analysis:
  model:
    class: "LegacySimulatorModel"
    params:
      version: "v7"

results:
  plots: []
  export:
    format: "json"

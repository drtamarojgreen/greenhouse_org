experiment_name: "v8_replication"
seed: 42
output_dir: "./results/v8"

data_collection:
  loader: "LegacyLoader"
  loader_params:
    version: "v8"

preprocessing:
  pipeline: []

analysis:
  model:
    class: "LegacySimulatorModel"
    params:
      version: "v8"

results:
  plots: []
  export:
    format: "json"

experiment_name: "v10_replication"
seed: 42
output_dir: "./results/v10"

data_collection:
  loader: "LegacyLoader"
  loader_params:
    version: "v10"

preprocessing:
  pipeline: []

analysis:
  model:
    class: "LegacySimulatorModel"
    params:
      version: "v10"

results:
  plots: []
  export:
    format: "json"
